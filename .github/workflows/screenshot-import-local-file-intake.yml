name: Screenshot Import Local File Intake

on:
  pull_request:
    paths:
      - ".github/workflows/screenshot-import-local-file-intake.yml"
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle.properties"
      - "specs/screenshot-import-local-file-intake.md"
      - "specs/screenshot-import-local-file-intake.test.md"
      - "specs/personal-stats-screenshot-import.md"
      - "specs/personal-stats-screenshot-import.test.md"
      - "src/main/kotlin/com/kingsmetric/importflow/**"
      - "src/test/kotlin/com/kingsmetric/importflow/**"
  push:
    branches:
      - main
      - feature/**
    paths:
      - ".github/workflows/screenshot-import-local-file-intake.yml"
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle.properties"
      - "specs/screenshot-import-local-file-intake.md"
      - "specs/screenshot-import-local-file-intake.test.md"
      - "specs/personal-stats-screenshot-import.md"
      - "specs/personal-stats-screenshot-import.test.md"
      - "src/main/kotlin/com/kingsmetric/importflow/**"
      - "src/test/kotlin/com/kingsmetric/importflow/**"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Run intake and importflow tests
        run: >
          ./gradlew --no-daemon :core:test
          --tests "com.kingsmetric.importflow.ScreenshotImportLocalFileIntakeTest"
          --tests "com.kingsmetric.importflow.ImportFeatureTest"
